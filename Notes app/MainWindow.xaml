<Window x:Class="Notes_app.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Notes_app"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <!-- Определяем строки для разметки -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Верхняя область -->
            <RowDefinition Height="*"/>
            <!-- Центральная область -->
            <RowDefinition Height="Auto"/>
            <!-- Нижняя область -->
        </Grid.RowDefinitions>

        <!-- Верхняя область для меню -->
        <Border Grid.Row="0" Background="LightGray" Height="50" Margin="10">
            <!-- Здесь будет меню -->
        </Border>

        <!-- Центральная область для текстового поля -->
        <Border Grid.Row="1" Background="White" Margin="10">
            <!-- Здесь будет основной контент -->
        </Border>

        <!-- Нижняя область для кнопок -->
        <Border Grid.Row="2" Background="LightGray" Height="50" Margin="10">
            <!-- Здесь можно разместить кнопки -->
        </Border>
    </Grid>
</Window>
